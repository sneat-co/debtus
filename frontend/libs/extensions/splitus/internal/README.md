# @sneat/extension-splitus-internal

The `internal` tier of the **splitus** extension (extension library-architecture
convention). See the repo root `README.md` → Frontend for the tier table.

## Running unit tests

Run `npx nx test extension-splitus-internal` to execute the unit tests.
