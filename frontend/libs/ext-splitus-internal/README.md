> ⚠️ **Scaffold.** This library was seeded from the `ext-debtus-*` template and
> renamed for Splitus. Its components/service are placeholders to give
> `splitus-app` a dedicated extension; replace them with real Splitus
> (bill-splitting) features.

# ext-splitus-internal

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test ext-splitus-internal` to execute the unit tests.
